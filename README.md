# Summerflow — Scrapbook Side Objects v4 — Fresh Run Fix

Use the provided runner instead of reusing an old browser tab.

- macOS: double-click `RUN_macOS.command`
- Windows: double-click `RUN_WINDOWS.bat`

The runner now chooses a fresh unused local port every time, preventing an older Summerflow server from being reopened accidentally.

This build contains the four Back_asset scrapbook objects and keeps the v15 phone/keyring/LCD behavior.
